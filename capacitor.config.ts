import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tuempresa.administracionestudiantes',
  appName: 'administracion-estudiantes',
  webDir: 'build'
};

export default config;
